# Wallpaper Engine Toolkit — unified bundle of Copier, Creator and Rotator.
#
# One GUI toolkit (PySide6/Qt) hosts all three features. The copy/build/rotate
# engines are the verbatim originals and add no dependencies of their own.

PySide6>=6.5          # unified Qt GUI (all tabs)
Pillow>=10.0          # Creator tab: preview thumbnails (optional — degrades gracefully)
imageio-ffmpeg>=0.4   # Auto Creator tab: bundled ffmpeg for generating preview.gif
pymongo>=4.6          # Review tab: the authors database
dnspython>=2.4        # ...which is reached over mongodb+srv, and that needs SRV lookups
pyinstaller>=6.0      # building the standalone .exe

# The Review tab talks to Steam over the standard library alone (urllib +
# sqlite3 for its cache), so nothing here is needed for that half of it.
